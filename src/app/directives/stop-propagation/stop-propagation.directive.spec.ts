import { StopPropagationDirective } from './stop-propagation.directive.ts.directive';

describe('StopPropagationDirectiveTsDirective', () => {
  it('should create an instance', () => {
    const directive = new StopPropagationDirective();
    expect(directive).toBeTruthy();
  });
});
